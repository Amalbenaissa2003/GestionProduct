{% extends 'base.html.twig' %}

{% block title %}Edit Supplier{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="page-title text-primary">
            <i class="bi bi-pencil-square me-2"></i> Edit Supplier
        </h1>
        <a href="{{ path('app_fournisseur_index') }}" class="btn btn-outline-secondary shadow-sm">
            <i class="bi bi-arrow-left me-1"></i> Back to List
        </a>
    </div>

    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white">
            <i class="bi bi-card-text me-2"></i> Supplier Information
        </div>
        <div class="card-body">
            {{ include('fournisseur/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>

    <div class="text-center mt-4">
        <a href="{{ path('app_fournisseur_index') }}" class="back-btn">
            <i class="bi bi-arrow-left-circle me-1"></i> Back to supplier list
        </a>

        {{ include('fournisseur/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}


